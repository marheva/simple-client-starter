import { TRANSLATIONS } from "../../constants";

const { FALLBACK_LANGUAGE, AVAILABLE_LANGUAGES } = TRANSLATIONS.LANGUAGES;

const getFallbackLanguage = FALLBACK_LANGUAGE;
const getAvailableLanguages = AVAILABLE_LANGUAGES;

export { getFallbackLanguage, getAvailableLanguages };
